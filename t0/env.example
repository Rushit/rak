# TensorZero & RAK Environment Variables
# 
# Setup Instructions:
# 1. Copy this file: cp env.example .env
# 2. Add your actual API keys and credentials below
# 3. Run: docker-compose up
#
# Note: .env is in .gitignore and will NOT be committed to git

# ============================================================
# API Keys
# ============================================================

# OpenAI API Key (required for TensorZero gateway)
OPENAI_API_KEY=sk-your-openai-key-here

# Gemini API Key (for RAK/Gemini integration)
GEMINI_API_KEY=your-gemini-api-key-here
GOOGLE_AI_STUDIO_API_KEY=your-gemini-api-key-here

# Anthropic API Key (optional)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ============================================================
# Database Configuration
# ============================================================

# ClickHouse credentials
# Default values are provided, but you should change them for production
CLICKHOUSE_USER=chusertoxi
CLICKHOUSE_PASSWORD=chpasswordtoxi$43

# Note: The $ character in passwords must be escaped as $$ in docker-compose
# but is used as a single $ in .env file

# Advanced: Override the full connection URL if needed
# TENSORZERO_CLICKHOUSE_URL=http://${CLICKHOUSE_USER}:${CLICKHOUSE_PASSWORD}@clickhouse:8123/tensorzero

# PostgreSQL credentials
# Used for TensorZero's application data storage
POSTGRES_USER=tensorzero
POSTGRES_PASSWORD=tensorzeropass
POSTGRES_DB=tensorzero

# ============================================================
# Gateway Configuration (optional overrides)
# ============================================================

# TENSORZERO_GATEWAY_URL=http://gateway:3000
